import { z } from 'zod';
import { RowTimeStampsSchema } from '../../types/models.types';
import { createAddSchema, createUpdateSchema } from '../../utils/typeUtils/schemaHelpers';

export const {{pascalCase name}}Schema = z
  .object({
    id: z.number(),
    // IMPORTANT: Fill in rest of object shape here
  })
  .extend(RowTimeStampsSchema);

export const {{pascalCase name}}AddSchema = createAddSchema({{pascalCase name}}Schema);
export const {{pascalCase name}}UpdateSchema = createUpdateSchema({{pascalCase name}}Schema);

export type {{pascalCase name}} = z.infer<typeof {{pascalCase name}}Schema>;
export type {{pascalCase name}}Add = z.infer<typeof {{pascalCase name}}AddSchema>;
export type {{pascalCase name}}Update = z.infer<typeof {{pascalCase name}}UpdateSchema>;

export default {{pascalCase name}};
