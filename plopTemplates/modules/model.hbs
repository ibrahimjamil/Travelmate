import BaseModel from '../../utils/moduleBase/base.model';
import { {{pascalCase name}} as {{pascalCase name}}Type, {{pascalCase name}}Schema } from './{{camelCase name}}.types';

// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface {{pascalCase name}} extends {{pascalCase name}}Type {}
// eslint-disable-next-line no-redeclare
export class {{pascalCase name}} extends BaseModel {
  static tableName = '{{plural (snakeCase name)}}';

  static get schema() {
    return {{pascalCase name}}Schema;
  }
}

export default {{pascalCase name}};